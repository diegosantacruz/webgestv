= simple_form_for(@parametro, :html => {:class => 'form-horizontal' }) do |f|
  %fieldset#mainform
    %legend.the-legend
      Parámetros compuestos
      =f.input :paracomp, label: 'Parámetros compuestos', :collection => @mcrsearch.map{ |c| [c.name,c.id] }, include_blank: 'Seleccione un parámetro', :input_html => {:onchange => "update_atr(this.value)"}
      #atrDiv=render  :partial => 'atrsearch', :link => @atrsearch
      #refreshatrDiv=render  :partial => 'refreshatr', :link => @refreshatr   

    .form-actions
      //%button.btn.btn-primary{:type => "submit"}=t('forms.load')
      =link_to "Cargar configuración", controller: "parametros", action: "show", id: @parametro.id
      //=link_to t('forms.back'), parametros_path, {:class => "parametro"}
      
     
     